spring:
  datasource: 
    url: jdbc:postgresql://localhost:5432/dbTestPhoneShop
    username: panhasak
    password: panhasak12345
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create 